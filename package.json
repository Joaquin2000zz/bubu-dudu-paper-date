{
  "name": "bubu-dudu-paper-date",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Animated 2D Bubu and Dudu in a native WebGL 3D garden",
  "scripts": {
    "start": "vite",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "build": "npm run typecheck && vite build && node scripts/build.cjs",
    "preview": "vite preview",
    "lint": "eslint src/**/*.ts",
    "format": "prettier --write src/**/*.ts tests/**/*.ts vite.config.ts",
    "test:browser": "node tests/joystick-browser.cjs && node tests/mobile-browser.cjs && node tests/couple-browser.cjs && node tests/standalone-browser.cjs",
    "check": "npm run typecheck && npm run lint && npm test && npm run build"
  },
  "devDependencies": {
    "eslint": "^10.10.0",
    "playwright": "^1.63.0",
    "prettier": "^3.9.6",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.70.0",
    "vite": "^8.2.2",
    "vitest": "^5.0.0"
  }
}
